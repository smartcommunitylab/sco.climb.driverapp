angular.module('driverapp.controllers.home', [])

.controller('HomeCtrl', function ($scope, APISrv) {
    /*
    APISrv.getSchools().then(
        function (schools) {
            console.log(schools);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getRoutesBySchool(testData.schoolId).then(
        function (routes) {
            console.log(routes);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getRouteById(testData.routeId).then(
        function (routes) {
            console.log(routes);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getStopsByRoute(testData.routeId).then(
        function (stops) {
            console.log(stops);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getChildrenBySchool(testData.schoolId).then(
        function (children) {
            console.log(children);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getChildrenBySchoolAndClass(testData.schoolId, testData.classRoom).then(
        function (children) {
            console.log(children);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getAnchors().then(
        function (anchors) {
            console.log(anchors);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getVolunteersBySchool(testData.schoolId).then(
        function (volunteers) {
            console.log(volunteers);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getVolunteersCalendarsBySchool(testData.schoolId).then(
        function (volunteersCalendars) {
            console.log(volunteersCalendars);
        },
        function (error) {
            console.log(error);
        }
    );
    */

    /*
    APISrv.getVolunteersCalendarsBySchoolAndVolunteer(testData.schoolId, testData.volunteerId).then(
        function (volunteerCalendars) {
            console.log(volunteerCalendars);
        },
        function (error) {
            console.log(error);
        }
    );
    */
});
