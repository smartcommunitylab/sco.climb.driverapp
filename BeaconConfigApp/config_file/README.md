This folder contains the configuration file for automatic Eddystone programming

The configuration file is an Excel sheet, but before import it in the app export it as CSV using ';' as field separator
